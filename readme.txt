La view boxdump se encarga de traer la consulta de todas las cajas creadas
para formar el tablon 

La conexion websocket esta completamente configurada y lista para usar.

El usuario "A" Puede enviar cambios a una instancia de el modelo Box
mientras que el usuario "B" contempla los cambios en tiempo real